{
  "name": "amritesh-lab",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "portfolio"
  ],
  "scripts": {
    "install": "npm install",
    "build": "cd portfolio && npm install && npm run build"
  }
}
